<!DOCTYPE html>
<html lang="en">
    <head>
    <%- include('./partials/head') %>
    </head>
<body>
    <%- include('./partials/script') %>
    <%- include('./partials/nav') %>
    <div class="container" style="width:auto">
        <h1>Title:<%= blog.title %></h1>
        <img src="/<%= blog.CoverImage %>" alt="..." style="width:50rem">
        <pre style="white-space: break-spaces;" class="mt-3"><%= blog.content %></pre>
        <div class="row m-4">
            <div class="col-4 border border-red">
                <img src="/<%= blog.createdBy.profilePicture%>" alt="" width="50px"> 
                <span>Author : <%= blog.createdBy.username %></span>
            </div>
        </div>
        <div class="row m-4">
            <h2>Comments (<%=blogComment.length%>):</h2>
            <div>
                <% blogComment.forEach(element => { %>
                 <div style="border-radius:4px 4px; border:1px solid;display:block; width: fit-content;padding:10px;margin:3px;">
                    <p><%=element.Content%></p>
                    <img src="/<%= element.createdBy.profilePicture%>" alt="" width="50px"> : <%=element.createdBy.username%>
                 </div>
                <% }) %>
            </div>
            <% if (locals.user) { %>
                <form action="/blog/Comment/<%= blog._id%>" method="post">
                    <div class="mb-3">
                        <label for="formGroupExampleInput3" class="form-label">Comment Section</label>
                      <textarea class="form-control" placeholder="Write Your Comments Hear" id="Comment" name="Comment"></textarea>
                      <input type="submit" class="btn btn-primary mt-2" name="Send">
                      </div>
                </form>
            <% } %>
            
        </div>
      </div>
</body>
</html>